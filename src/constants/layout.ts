/**
 * Layout constants for responsive design
 */

/**
 * Height threshold in pixels below which compact layout is activated
 * - Mobile landscape mode (typically 400-500px)
 * - Small desktop windows
 * But NOT mobile portrait (typically >667px)
 */
export const COMPACT_LAYOUT_HEIGHT_THRESHOLD = 600
