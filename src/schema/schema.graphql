enum Status {
  MEET_NEW_PEOPLE
  CHAT
  WANT_TO_SPEAK_OUT
  WANT_TO_LISTEN
  NEED_HELP_WITH_SITUATION
  WANT_TO_HELP_WITH_SITUATION
}

type User {
  name: String!
  ip: String!
  status: [Status!]!
}

type Query {
  users: [User!]!
} 